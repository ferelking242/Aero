<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="160dp"
    android:layout_margin="5dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:strokeWidth="2dp"
    app:strokeColor="@android:color/transparent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Thumbnail / preview area -->
        <View
            android:id="@+id/tabPreviewArea"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@color/color_tab_preview_bg"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tabFooter" />

        <!-- Incognito icon centered in preview -->
        <ImageView
            android:id="@+id/ivIncognitoCenter"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_incognito"
            android:tint="@color/color_incognito"
            android:visibility="gone"
            android:contentDescription="@string/incognito"
            app:layout_constraintTop_toTopOf="@id/tabPreviewArea"
            app:layout_constraintBottom_toBottomOf="@id/tabPreviewArea"
            app:layout_constraintStart_toStartOf="@id/tabPreviewArea"
            app:layout_constraintEnd_toEndOf="@id/tabPreviewArea" />

        <!-- Group badge (shown when tab is in a group) -->
        <TextView
            android:id="@+id/tvGroupBadge"
            android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:visibility="gone"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:textSize="10sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:ellipsize="end"
            android:background="@drawable/bg_group_badge"
            android:layout_marginStart="8dp"
            android:layout_marginBottom="4dp"
            app:layout_constraintBottom_toTopOf="@id/tabFooter"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Footer bar with title + close -->
        <LinearLayout
            android:id="@+id/tabFooter"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="10dp"
            android:paddingEnd="6dp"
            android:background="@color/color_toolbar"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:id="@+id/ivIncognito"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_incognito"
                android:tint="@color/color_incognito"
                android:visibility="gone"
                android:contentDescription="@string/incognito"
                android:layout_marginEnd="4dp" />

            <TextView
                android:id="@+id/tvTabTitle"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/color_text_primary"
                android:maxLines="1"
                android:ellipsize="end" />

            <ImageButton
                android:id="@+id/btnCloseTab"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_close"
                android:tint="@color/color_icon_secondary"
                android:contentDescription="@string/close_tab" />

        </LinearLayout>

        <!-- Hidden URL text view (still needed for adapter) -->
        <TextView
            android:id="@+id/tvTabUrl"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
